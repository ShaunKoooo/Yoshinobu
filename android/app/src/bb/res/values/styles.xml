<resources>

    <!-- BB App theme with launch screen -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- BB App Launch Screen -->
        <item name="android:windowBackground">@drawable/splashscreen_custom</item>
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    </style>

</resources>